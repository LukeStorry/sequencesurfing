---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<li class="">
	<a href={href} class="">
		<h2 class="">{title}<span>&rarr;</span></h2>
		<p class="">{body}</p>
	</a>
</li>

